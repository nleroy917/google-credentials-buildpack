#!/bin/sh

# this pack is valid for apps with a GOOGLE_CREDENTIALS env
# variable in the project
if [[ -z "${GOOGLE_CREDENTIALS}"/ ]]; then
  echo "-----> No environement variable GOOGLE_CREDENTIALS detected."
  exit 1
else
  exit 0
fi

